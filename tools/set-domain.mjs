import fs from 'node:fs';import path from 'node:path';const domain=(process.argv[2]||'').replace(/\/$/,'');if(!/^https:\/\//.test(domain)){console.error('Usage: node tools/set-domain.mjs https://example.pages.dev');process.exit(1)}const root=process.cwd();for(const rel of ['assets/js/config.js','robots.txt','sitemap.xml']){const p=path.join(root,rel);let s=fs.readFileSync(p,'utf8');s=s.replaceAll('https://rebornrank.pages.dev',domain);fs.writeFileSync(p,s)}console.log('Domain updated:',domain);